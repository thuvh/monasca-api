---
features:
  - |
    Configuration option `db_per_tenant` added for InfluxDB to allow
    data points to be read from dedicated tenant database where the
    `database_name` prefixes the tenant ID, e.g. monasca_tenantid.
